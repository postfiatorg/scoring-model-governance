"""Byte-identical copies of dynamic-unl-scoring source, kept for hash provenance.

Files in this directory are never imported at runtime: they carry foundation
imports (xrpl) that this service does not depend on. The runnable adaptation
lives in governance_service.scoring.hashing, and the content pins in
governance_service.scoring assert that these copies still match the upstream
files they were taken from.
"""
